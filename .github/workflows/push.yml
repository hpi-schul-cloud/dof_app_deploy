---
name: Auto-deployment for Branches
run-name: dev deployment of ${{ github.head_ref }} by @${{ github.actor }}

on:
  push:
    branches-ignore:
      - dependabot/**

jobs:
  branch_name:
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.extract_branch.outputs.branch }}
    steps:
      - name: Extract branch name
        shell: bash
        run: echo "branch=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT
        id: extract_branch

  deploy:
    needs:
      - branch_name
    uses: ./.github/workflows/deploy.yml
    with:
      branch: ${{ needs.branch_name.outputs.branch }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
      DEV_VAULT: ${{ secrets.DEV_VAULT }}
      DEV_KUBE_CONFIG: ${{ secrets.DEV_KUBE_CONFIG }}
      TOKEN_INVENTORY: ${{ secrets.TOKEN_INVENTORY }}

  deploy-successful:
    needs:
      - deploy
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy was successful"
