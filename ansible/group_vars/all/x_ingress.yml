default_ingress:
  default:
    path: /
    serviceName: client-svc
    servicePort: "{{ CLIENT_PORT }}"
  themes:
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
    path: "/favicon.png"
  runtime:
    path: /runtime.config.json
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  activation:
    path: /activation
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  metrics:
    path: /metrics
    serviceName: default-backend-404-svc
    servicePort: 8080
  login:
    path: /login
    serviceName: client-svc
    servicePort: "{{ CLIENT_PORT }}"
  login_success:
    path: /login/success
    serviceName: client-svc
    servicePort: "{{ CLIENT_PORT }}"
  error:
    path: /error
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  imprint:
    path: /imprint
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  mint-ec:
    path: /mint-ec
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  news:
    path: /news
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  tasks:
    path: /tasks
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  nuxtversion:
    path: /nuxtversion
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  content:
    path: /content
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  _nuxt:
    path: /_nuxt
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_ldap:
    path: /administration/ldap
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_migration:
    path: /administration/migration
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_school-settings:
    path: /administration/school-settings
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration/datasources:
    path: /administration/datasources
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_students:
    path: /administration/students
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_teachers:
    path: /administration/teachers
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  administration_groups_classes:
    path: /administration/groups/classes
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  rooms-overview:
    path: /rooms-overview
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  rooms-list:
    path: /rooms-list
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  rooms:
    path: /rooms
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  h5p-player:
    path: /h5p/player
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
    with: WITH_H5P_EDITOR
  h5p-editor:
    path: /h5p/editor
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
    with: WITH_H5P_EDITOR
  cfiles:
    path: /cfiles
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  migration:
    path: /migration
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  tools_context:
    path: /tools/context
    serviceName: nuxtclient-svc
    servicePort: "{{ NUXTCLIENT_PORT }}"
  api:
    path: /api
    serviceName: default-backend-404-svc
    servicePort: 8080
  api_v1_alert:
    path: /api/v1/alert
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_accounts_jwt-timer:
    path: /api/v1/accounts/jwtTimer
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_edu-sharing:
    path: /api/v1/edu-sharing
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_me:
    path: /api/v1/me
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_tasks:
    path: /api/v3/tasks
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_rooms:
    path: /api/v3/rooms
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_boards:
    path: /api/v3/boards
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_columns:
    path: /api/v3/columns
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_cards:
    path: /api/v3/cards
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_elements:
    path: /api/v3/elements
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_submissionItems:
    path: /api/v3/board-submissions
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_user_me:
    path: /api/v3/user/me
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_user_import:
    path: /api/v3/user/import
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_user_language:
    path: /api/v3/user/language
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_news:
    path: /api/v3/news
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_dashboard:
    path: /api/v3/dashboard
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_courses:
    path: /api/v3/courses
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_lessons:
    path: /api/v3/lessons
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_school:
    path: /api/v3/school
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_submissions:
    path: /api/v3/submissions
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_sharetoken:
    path: /api/v3/sharetoken
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_sso_login:
    path: /api/v3/sso/login/
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_sso_oauth:
    path: /api/v3/sso/oauth/
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_sso_hydra:
    path: /api/v3/sso/hydra/
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_tools:
    path: /api/v3/tools
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_user-migration:
    path: /api/v3/user-migration
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_user-login-migration:
    path: /api/v3/user-login-migrations
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_system:
    path: /api/v3/systems/public
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_videoconference2:
    path: /api/v3/videoconference2
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_groups:
    path: /api/v3/groups
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_pseudonyms:
    path: /api/v3/pseudonyms
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_roster:
    path: /api/v1/roster
  api_roster:
    path: /api/roster
  api_v1_schools:
    path: /api/v1/schools/
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_federalStates:
    path: /api/v1/federalStates
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_consentVersions:
    path: /api/v1/consentVersions
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_systems:
    path: /api/v1/systems
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_ldap-config:
    path: /api/v1/ldap-config
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_version:
    path: /serverversion
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_wopi:
    path: /api/v1/wopi
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_courses:
    path: /api/v1/courses
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_lessons:
    path: /api/v1/lessons
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_authentication:
    path: /api/v1/authentication
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_ldap:
    path: /api/v1/ldap
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_registration:
    path: /api/v1/registration
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_users:
    path: /api/v1/users
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_classes:
    path: /api/v1/classes
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_roles_user:
    path: /api/v1/roles/user
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v1_config_app_public:
    path: /api/v1/config/app/public
    serviceName: api-svc
    servicePort: "{{ PORT_SERVER }}"
  api_v3_file_upload:
    path: /api/v3/file/upload
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_upload_from_url:
    path: /api/v3/file/upload-from-url
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_download:
    path: /api/v3/file/download
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_list:
    path: /api/v3/file/list
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_rename:
    path: /api/v3/file/rename
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_delete:
    path: /api/v3/file/delete
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_restore:
    path: /api/v3/file/restore
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_copy:
    path: /api/v3/file/copy
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_file_preview:
    path: /api/v3/file/preview
    serviceName: api-files-svc
    servicePort: "{{ PORT_FILE_SERVICE }}"
  api_v3_h5p:
    path: /api/v3/h5p-editor
    serviceName: api-h5p-svc
    with: WITH_H5P_EDITOR
  etherpad:
    path: /etherpad
    serviceName: etherpad-nginx-svc
    servicePort: 80
  etherpad_socket.io:
    path: /etherpad/socket.io
    serviceName: etherpad-nginx-svc
    servicePort: 80
  version_aggregator:
    path: /version
    serviceName: version-aggregator-svc
    servicePort: 8080
  h5p-statics:
    path: /h5pstatics
    with: WITH_H5P_EDITOR
  tldraw_client:
    path: /tldraw
    serviceName: tldraw-client-svc
    servicePort: "{{ PORT_TLDRAW_CLIENT }}"
  tldraw_client_runtime_config:
    path: /tldraw-client-runtime.config.json
    serviceName: tldraw-client-svc
    servicePort: "{{ PORT_TLDRAW_CLIENT }}"
  tldraw_server:
    path: /tldraw-server
    serviceName: tldraw-svc
    servicePort: "{{ PORT_TLDRAW }}"
  static:
    path: /static
    serviceName: tldraw-client-svc
    servicePort: "{{ PORT_TLDRAW_CLIENT }}"