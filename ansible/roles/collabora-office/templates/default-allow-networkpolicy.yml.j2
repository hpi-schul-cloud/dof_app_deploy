apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: collabora-office-default-allow
  namespace: {{ NAMESPACE }}
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - collabora-office
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchExpressions:
              - key: kubernetes.io/metadata.name
                operator: In
                values:
                  - monitoring
                  - ingress-controller
                  - scaling
                  - "{{ NAMESPACE }}"
