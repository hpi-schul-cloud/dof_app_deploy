apiVersion: v1
kind: Service
metadata:
  name: tldraw-nginx-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: tldraw-nginx
  selector:
    app: tldraw-nginx
