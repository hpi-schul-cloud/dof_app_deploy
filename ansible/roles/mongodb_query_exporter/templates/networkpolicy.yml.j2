apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mongodb-query-exporter-allow-prometheus
  namespace: {{ NAMESPACE }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mongodb-query-exporter
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: monitoring
    ports:
    - protocol: TCP
      port: 9412

