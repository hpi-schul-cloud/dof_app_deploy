apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: maildrop-secret
  namespace: {{ NAMESPACE }}
  labels:
    app: api
spec:
  itemPath: "vaults/{{ ONEPASSWORD_OPERATOR_VAULT }}/items/maildrop"
