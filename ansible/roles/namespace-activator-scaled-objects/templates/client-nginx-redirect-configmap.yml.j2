apiVersion: v1
kind: ConfigMap
metadata:
  name: client-nginx-redirect
  namespace: {{ NAMESPACE }}
data:
  default.conf: |
    server {
      listen 3100;
      server_name _;
      location / {
        return 302 "https://activate.{{ BASE_DOMAIN }}/namespace?namespace={{ NAMESPACE }}&redirected-from-503=true";
      }
    }

