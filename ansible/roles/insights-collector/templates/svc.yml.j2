apiVersion: v1
kind: Service
metadata:
  name: insights-collector-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: insights-collector
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: insights-collector
  selector:
    app: insights-collector
