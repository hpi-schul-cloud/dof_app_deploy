apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cachet-postgres-pvc
  namespace: {{ NAMESPACE }}
  labels:
    app: cachet-postgres
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ POSTGRES_CAPACITY|default("5Gi", true) }}