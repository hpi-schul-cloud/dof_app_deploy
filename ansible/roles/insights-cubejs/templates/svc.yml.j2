apiVersion: v1
kind: Service
metadata:
  name: insights-cubejs-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: insights-cubejs
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: insights-cubejs
  selector:
    app: insights-cubejs
