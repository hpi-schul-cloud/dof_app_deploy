apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: rocketchat-secret{{ EXTERNAL_SECRETS_POSTFIX if WITH_BRANCH_MONGO_DB_MANAGEMENT: false else '' }}
  namespace: {{ NAMESPACE }}
  labels:
    app: rocketchat
spec:
  itemPath: "vaults/{{ ONEPASSWORD_OPERATOR_VAULT }}/items/rocketchat"
