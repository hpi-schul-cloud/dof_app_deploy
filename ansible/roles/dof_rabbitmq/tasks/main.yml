  - name: RabbitMQ definitions
    community.kubernetes.k8s:
      kubeconfig: ~/.kube/config
      namespace: "{{ NAMESPACE }}"
      template: rabbitmq-definitions-config.yaml.j2


  - name: RabbitMQ Cluster
    community.kubernetes.k8s:
      kubeconfig: ~/.kube/config 
      namespace: "{{ NAMESPACE }}"
      template: rabbitmq.yaml.j2

      
