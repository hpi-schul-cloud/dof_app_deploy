apiVersion: v1
kind: Service
metadata:
  name: etherpad-nginx-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: etherpad-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: etherpad-nginx
  selector:
    app: etherpad-nginx
