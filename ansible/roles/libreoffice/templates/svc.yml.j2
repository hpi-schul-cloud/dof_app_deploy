apiVersion: v1
kind: Service
metadata:
  name: libreoffice-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: libreoffice
spec:
  type: ClusterIP
  ports:
    - port: {{ PORT_LIBREOFFICE}}
      targetPort: 9980
      protocol: TCP
      name: libreoffice
  selector:
    app: libreoffice
