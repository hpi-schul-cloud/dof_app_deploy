apiVersion: v1
kind: Service
metadata:
  name: api-h5p-proxy-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: api-h5p-proxy
spec:
  type: ClusterIP
  ports:
  - port: {{ H5P_PROXY_PORT }}
    targetPort: 80
    protocol: TCP
    name: api-h5p-proxy
  selector:
    app: api-h5p-proxy
